spring:
  datasource:
    password: password
    username: postgres
    url: jdbc:postgresql://host.docker.internal:5432/postgres?currentSchema=public
  sql:
    init:
      mode: always
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql

